apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chromadb-pvc
  namespace: resumeai
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-auto
  resources:
    requests:
      storage: 5Gi
